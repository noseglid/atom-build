describe "package", ->
  describe "when all packages are activated", ->
    it "discovers build systems", ->
    it "installs handler for project path change", ->
  describe "when project path changes", ->
    it "updates build systems", ->
